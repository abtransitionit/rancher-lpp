# clustered resources

apiVersion: v1
kind: Namespace
metadata:
  name: lpp

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: lpp-role
rules:
  - apiGroups: [""]
    resources: ["nodes", "persistentvolumeclaims", "configmaps", "pods", "pods/log"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["persistentvolumes"]
    verbs: ["get", "list", "watch", "create", "patch", "update", "delete"]
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create", "patch"]
  - apiGroups: ["storage.k8s.io"]
    resources: ["storageclasses"]
    verbs: ["get", "list", "watch"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: lpp-bind
  namespace: lpp
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: lpp-role
subjects:
  - kind: ServiceAccount
    name: lpp-sa
    namespace: lpp

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: lpp-bind
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: lpp-role
subjects:
  - kind: ServiceAccount
    name: lpp-sa
    namespace: lpp

---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: local-path
# When someone asks for storage with this StorageClass, call the component that registered itself under this exact name 
# the id rancher.io/local-path is hard coded in the container image of the LPP.
provisioner: rancher.io/local-path
volumeBindingMode: WaitForFirstConsumer
reclaimPolicy: Delete


# namespaced resources

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: lpp-sa
  namespace: lpp

---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: lpp-role
  namespace: lpp
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch", "create", "patch", "update", "delete"]

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lpp
  namespace: lpp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lpp
  template:
    metadata:
      labels:
        app: lpp
    spec:
      serviceAccountName: lpp-sa
      containers:
        - name: lpp
          image: rancher/local-path-provisioner:v0.0.34
          imagePullPolicy: IfNotPresent
          command:
            - local-path-provisioner
            - --debug
            - start
            - --config
            - /etc/config/config.json
          volumeMounts:
            - name: config-volume
              mountPath: /etc/config/
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: CONFIG_MOUNT_PATH
              value: /etc/config/
      volumes:
        - name: config-volume
          configMap:
            name: local-path-config


---
kind: ConfigMap
apiVersion: v1
metadata:
  name: local-path-config
  namespace: lpp
data:
  config.json: |-
    {
            "nodePathMap":[
            {
                    "node":"DEFAULT_PATH_FOR_NON_LISTED_NODES",
                    "paths":["/wkspc/rancher/lpp"]
            }
            ]
    }
  setup: |-
    #!/bin/sh
    set -eu
    mkdir -m 0777 -p "$VOL_DIR"
  teardown: |-
    #!/bin/sh
    set -eu
    rm -rf "$VOL_DIR"
  helperPod.yaml: |-
    apiVersion: v1
    kind: Pod
    metadata:
      name: helper-pod
    spec:
      priorityClassName: system-node-critical
      tolerations:
        - key: node.kubernetes.io/disk-pressure
          operator: Exists
          effect: NoSchedule
      containers:
      - name: helper-pod
        image: busybox
        imagePullPolicy: IfNotPresent
